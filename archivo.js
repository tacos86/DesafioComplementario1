

const iva = 1.21;
const precio =(prompt("Ingresar el valor del producto"));

const precioFinal = iva * precio;


document.write ("El importe final es $ " + precioFinal);

/*const precioFinal = 
  function calcular (iva, precio) {
    return iva * precio;    
}

document.write("El Importe total es $ " + precioFinal)*/